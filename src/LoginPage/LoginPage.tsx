import React from 'react';
import { Container, Paper } from '@material-ui/core';

const LoginPage = () => (
  <Container maxWidth="sm" style={{ height: '100%' }}>
    <Paper elevation={3} style={{ backgroundColor: '#64c1a6', height: '33%' }}>
      Login
    </Paper>
  </Container>
);

export default LoginPage;
